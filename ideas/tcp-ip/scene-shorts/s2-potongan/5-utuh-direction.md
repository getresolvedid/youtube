Anggaran: dihitung `npm run gen` — lihat `timing.gen.ts`.
Satu baris VO = satu tahap.

Tutup Short 2, dan sambungan loop-nya.

1. lima potongan yang sudah urut merapat ke tengah dan MENYATU jadi satu bidang
   utuh — bidang yang sama persis dengan yang membentur di scene 1: ukuran sama,
   warna sama, tulisan `FILE` sama. tujuan di bawahnya menyala.
   rangkuman satu baris muncul: `banyak potongan kecil = satu file`.
   (VO: "Jadi, saat kamu mengirim data melalui internet…")

motion:
   - merapat: `t()` pada x kelimanya menuju W/2, `E.power2out`
   - menyatu: opacity potongan → 0 dan opacity bidang → 1 di titik yang SAMA,
     durasi 0,4, tanpa geser
   - rangkuman: `masuk()` dengan `geser` 22
   - tujuan menyala: `t()` 0 → 1

catatan:
   - **bidangnya wajib identik dengan scene 1.** ukuran, warna, dan tulisannya.
     itu yang menutup lingkarannya: yang tadi tidak muat sekarang sudah sampai,
     utuh. bidang yang sedikit berbeda membuat penonton mengira ini file lain.
   - **frame terakhir harus DIAM** sebelum `99-closing` mengambil alih.
   - loop-nya lewat bidang itu, bukan lewat kalimat — frame terakhir Short ini
     dan frame pertamanya adalah benda yang sama.
