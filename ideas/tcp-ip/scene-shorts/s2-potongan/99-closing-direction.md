Tanda brand penutup — `CLOSING_SHORT_SECONDS` dari `.env` (2,0 dtk). **Tidak ada
VO** (docs/10), jadi tidak ada berkas `-vo.md`.

Komponennya `<TandaBrand rasio="9x16" />` dari `shared/StandarScenes.tsx`,
berlaku untuk semua Short di semua topik — jangan diubah dari sini.

**Frame terakhir `5-utuh` harus sudah diam** sebelum kartu ini mengambil alih.
Di Short ini itu penting sekali: frame terakhirnya adalah bidang `FILE` yang
sama dengan frame pertama Short, dan seluruh loop-nya bergantung pada bidang itu
berdiri tenang saat penonton melihatnya untuk kedua kalinya.
