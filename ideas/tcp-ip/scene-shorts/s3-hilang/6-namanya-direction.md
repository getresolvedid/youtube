Anggaran: dihitung `npm run gen` — lihat `timing.gen.ts`.
Satu baris VO = satu tahap.

Tutup Short 3.

1. kelima potongan merapat ke tengah jadi satu blok padat. di atasnya berdiri
   nama dan pekerjaannya: `TCP` / `lengkap & urut`.
   (VO: "Jadi, TCP membantu membuat komunikasi data menjadi lebih andal.")

motion:
   - merapat: `t()` pada x kelimanya menuju posisi rapat, `E.power2out`, 0,8 dtk
   - rangkuman: `masuk()` dengan `geser` 24, tertunda 0,5 dtk setelah merapat
   - sesudah itu DIAM sampai scene habis

catatan:
   - **kepanjangan "Transmission Control Protocol" tidak dipakai di Short ini.**
     ia tidak menambah satu pun gambaran, dan di 9:16 ia memakan dua baris penuh
     yang lebih berguna untuk pekerjaannya.
   - **loop-nya lewat barisan lima potongan.** frame terakhir Short ini dan frame
     pertamanya sama-sama lima potongan bernomor.
   - **frame terakhir harus DIAM** sebelum `99-closing` mengambil alih.
